<h1> Tags </h1>
<p>
<% @tags.each do |t| %>
  <li>
<%= link_to t.name, tag_path(t), class: 'article_title' %>
</li>
<br/>
<%= link_to "delete", tag_path(t), method: :delete, data: {confirm: "Are you sure you want to delete the tag?"} %> <br/>
<%= link_to "edit", edit_tag_path(t) %>
  <% end %>
</p>

